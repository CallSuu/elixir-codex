# 엘릭서 코덱스 (Elixir Codex) 🧪

> "신비로운 연금술 가마솥에 나의 일상 습관과 영양제를 넣어, 전설의 비약(Elixir)을 연성하다"
> 일상 습관·영양제 인증을 재료 삼아 AI가 엘릭서를 연성하는 웰니스 덱빌딩 & 수집형 앱입니다.

---

## 📌 프로젝트 소개

**엘릭서 코덱스**는 고풍스러운 중세 판타지 세계관(NPC 늘해랑과 부상당한 용사의 건강 회복 스토리) 위에서, 실제 건강 습관과 영양제 섭취를 게임화한 서비스입니다.

일일/주간 퀘스트로 재료를 모으고, 실제 섭취한 영양제를 사진으로 인증하면 그날의 재료가 가마솥에 자동으로 모입니다. AI가 그 재료를 바탕으로 세상에 하나뿐인 엘릭서 카드를 연성해주고, 유저는 이걸 도감에 수집하며 성장합니다.

---

## 🛠 기술 스택

- **Backend**: Spring Boot 4.1(Framework 7), Java 17, Gradle
- **DB**: MySQL (JPA/Hibernate), 테스트는 H2 인메모리로 분리
- **인증**: Spring Security + JWT
- **AI**: OpenAI GPT-4o (텍스트 생성·비전·이미지 생성)
- **플랫폼**: App (모바일, iOS/Android)

---

## 🔑 핵심 기능

### 인증
회원가입 · 로그인 · 로그아웃 · 내 정보 조회

### 퀘스트
일일 퀘스트(자동 배정, 수동 완료 + Health 데이터 자동 판정) · 주간 퀘스트(도메인 기반 자동 배정)

### 재료 · 영양제 인증
재료 인벤토리 · 가마솥 스택 조합(당일 인증 영양제 자동 편입) · 영양제 인증 사진 업로드(GPT-4o 비전으로 제품명·신뢰도 판정)

### 연금술 연성
GPT-4o 명칭·조언 문구 생성 · 카드 아트(사전 템플릿 매칭 + AI 실시간 생성 폴백) · 등급 산정(점수제 + 확률 업그레이드 + 프리즘 레전더리) · 카드 수치형 스탯 · 돌연변이 판정

### 도감 · 마이페이지
도감 목록/상세 조회 · 돌연변이 전용 도감 · 비밀 서재 카드 배치

### 온보딩 · 스페셜 엘릭서
온보딩 텍스트 AI 도메인 분류 · 스페셜 엘릭서 생성(최대 3개 제한)

### 출석체크
연속 출석 판정 · 7일마다 가구 보상

---

## 💻 로컬 실행 방법

### 1. MySQL 준비

```sql
CREATE DATABASE elixir_codex;
```

### 2. 환경변수 설정

IntelliJ 실행 설정(`DemoApplication`)의 Environment variables에 아래 값을 추가합니다.

DB_USERNAME=본인_mysql_아이디
DB_PASSWORD=본인_mysql_비밀번호
OPENAI_API_KEY=본인_OpenAI_API_키
JWT_SECRET=32자_이상의_임의의_문자열

### 3. 실행

`DemoApplication`을 IntelliJ에서 실행하거나, 터미널에서 아래 명령을 실행합니다.

./gradlew bootRun

정상 실행되면 `http://localhost:8080`에서 서버가 동작합니다.

### 4. 테스트

./gradlew test

테스트는 실제 MySQL이 아닌 H2 인메모리 DB를 사용하도록 분리되어 있어, 개발용 데이터베이스에 영향을 주지 않습니다.

---

## 📄 API 문서

각 기능별 상세 API 명세는 `docs/` 폴더에 기능 단위로 정리되어 있습니다(공통 사항 → API 목록 → 요청/응답/오류 → 데이터 구조 순서로 통일된 형식). 새 기능을 추가할 때마다 이 규칙에 따라 문서를 같이 작성/갱신합니다(`CLAUDE.md` 참고).

Postman으로 전체 기능을 순서대로 확인하고 싶다면 정리된 테스트 가이드를 참고하세요.

---

## 🌿 브랜치 전략

- `main`: 항상 안정적으로 동작하는 버전만 유지
- `develop`: 두 백엔드 담당자의 작업을 통합하는 브랜치
- `backend-1`, `backend-2`: 각자 작업은 별도 브랜치에서 진행한 뒤 develop으로 병합
- `feature/기능명`: 개별 기능 단위 작업 시 사용

---

## 👥 팀 구성

- Backend: 2명
- Frontend: 1명
- Design: 1명
- 기획 & 데이터: 1명

---

## 📝 프로젝트 한 줄 소개

**엘릭서 코덱스는 일상 습관과 영양제 인증을 재료 삼아 AI가 나만의 엘릭서를 연성해주는 웰니스 덱빌딩 & 수집형 앱입니다.**